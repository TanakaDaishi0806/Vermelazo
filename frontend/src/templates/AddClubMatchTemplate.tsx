import Header from "../components/Header";
import Calendar from "../components/Calendar";

const AddClubMatchTemplate = () => {
  return (
    <div>
      <Header headertext={{ text: "Admin Page" }} />
      <Calendar />
    </div>
  );
};

export default AddClubMatchTemplate;
